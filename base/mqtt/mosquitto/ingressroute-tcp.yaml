apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: mosquitto-route
  namespace: mqtt
spec:
  entryPoints:
    - mqtt
  routes:
    - kind: Rule
      match: HostSNI(`*`)
      services:
        - name: mosquitto
          port: 1883
