apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - pvc.yaml
  - datasources.yaml
  - sealed-secret.yaml
  - deployment.yaml
  - service.yaml
  - certificate.yaml
  - ingress.yaml