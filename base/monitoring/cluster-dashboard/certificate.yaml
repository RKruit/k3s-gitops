apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cluster-dashboard-tls
  namespace: monitoring
  labels:
    app: cluster-dashboard
spec:
  dnsNames:
    - dashsv02.oostrandpark.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: cluster-dashboard-tls