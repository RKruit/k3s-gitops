apiVersion: v1
kind: Service
metadata:
  name: cluster-dashboard
  namespace: monitoring
  labels:
    app: cluster-dashboard
spec:
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app: cluster-dashboard