apiVersion: v1
kind: Service
metadata:
  name: cluster-dashboard
  namespace: monitoring
  labels:
    app: cluster-dashboard
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: cluster-dashboard