apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: todo-app-tls
  namespace: todo-app
spec:
  dnsNames:
    - todo.oostrandpark.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: todo-app-tls