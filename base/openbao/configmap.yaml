apiVersion: v1
kind: ConfigMap
metadata:
  name: openbao-config
  namespace: openbao
data:
  config.hcl: |
    disable_cache = true
    telemetry {
      prometheus_retention_time = "24h"
      disable = false
    }
    storage "file" {
      path = "/openbao/data"
    }
    listener "tcp" {
      address = "0.0.0.0:8200"
      tls_disable = true
    }
    api_addr = "http://127.0.0.1:8200"
    cluster_addr = "http://127.0.0.1:8201"
    max_request_concurrent = 0
    default_lease_ttl = "768h"
    max_lease_ttl = "8760h"