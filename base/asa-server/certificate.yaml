apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: asa-server-tls
  namespace: asa-server
spec:
  dnsNames:
  - asa.oostrandpark.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: asa-server-tls
