apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: it-tools-tls
  namespace: it-tools
spec:
  dnsNames:
    - tools.oostrandpark.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: it-tools-tls
